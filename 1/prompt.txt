There was supposed to have been logic added in a recent session to be able to map out the placement of the modules on the Zoia LED grid. It looks, based on the preview visualization for page 1, like it's doing auto-assignment, which is fine, but how do I edit that?

---

What's missing is the ability to drag to the grid location I want. I also don't like the idea of pinning it. If it's been placed somewhere, either manually or automatically, it doesn't get moved around. The exception is that if you drag something into a place that overlaps it, it moves out of the way (and returns to its spot if you move your placement elsewhere). Similarly, if an object next to it has additional parameters enabled (and thus more LEDs on the grid), it should move out of the way to accommodate that. I also noticed that if I highlight an element and use the Cmd+Del keyboard shortcut, the inspector on the right goes away, but the module itself stays. It won't delete unless I right-click and delete.

---

I tried to delete with the shortcut and it threw an index out of range error.

Swift/ContiguousArrayBuffer.swift:695: Fatal error: Index out of range
Type: stdio

---

Swift/ContiguousArrayBuffer.swift:695: Fatal error: Index out of range
Type: stdio

---

That looks like it's working. Next up, I like the animations, but they need to be a little bit more subtle. The sine waves on the audio wires, for example, are too big. The pulsing that's happening on the clock is hard and jarring.

---

Much better. Good work. Are there other animations that I should see to judge whether they need similar adjustment?

---

Create a patch that has all the animations in it. Another change that I see is that I need to be able to drag/disconnect the wires from one element and move them to another. For example, I added an audio panner and wanted to move the vca output going to the speakers over to the panner instead, and that wasn't possible. It required a delete

---

Move AnimationDemo.bin to my Desktop